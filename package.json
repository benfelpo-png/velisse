{
  "name": "fabricops",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:app && npm run test:api",
    "test:app": "node tests/app.smoke.test.mjs",
    "test:api": "node tests/api.integration.test.mjs",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  },
  "devDependencies": {
    "pg": "^8.13.1"
  }
}
